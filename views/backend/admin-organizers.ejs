<%include ../partials/header%>
<% include ../partials/sidebar-admin%>
<div id="page-wrapper">
    <div class="row">
        <div class="col-lg-12">

            <%if(errorDeleteAccountMessage.length == 0){%>
            <p style="color: green" id="successMessage"><%=successDeleteAccountMessage%></p>
            <%}else{%>
            <p style="color: red" id="errorMessage"><%=errorDeleteAccountMessage%></p>
            <%}%>

            <h1 class="page-header">Jucatori</h1>
            <h4>Exista <%= organizers.length > 1 ? organizers.length + ' organizatori' : organizers.length + 'organizator'%> in baza de date</h4>
        </div>
        <!-- /.col-lg-12 -->
    </div>
    <!-- /.row -->
    <div class="row">
        <div class="col-lg-12">
            <!-- /.panel -->
            <div class="panel panel-default">
                <div class="panel-heading">
                    <i class="fa fa-clock-o fa-fw"></i> Lista Organizatori
                </div>
                <!-- /.panel-heading -->
                <div class="panel-body">
                    <table class="table table-hover">
                        <tr>
                            <th>Nickname</th>
                            <th>Battle.net ID</th>
                            <th>Rasa</th>
                            <th>Liga</th>
                            <th>Profil</th>
                            <th>Turnee organizate</th>
                            <th>Sterge contul</th>
                        </tr>

                        <% organizers.forEach(function(organizer){ %>
                        <tr>
                            <td><%= organizer.local.nickname %></td>
                            <td><%= organizer.local.battlenetid %></td>
                            <td><%= organizer.local.race %></td>
                            <td><%= organizer.local.league %></td>
                            <td>
                                <a href="/profile-details/<%= organizer._id %>">
                                    <button class="btn btn-success">Detalii</button>
                                </a>
                            </td>
                            <td>
                                <a href="/organized-tournaments/<%= organizer._id %>">
                                    <button class="btn btn-info">Vezi turneele</button>
                                </a>
                            </td>
                            <td>
                                <form action="/delete-account/<%= organizer._id %>" method="post">
                                    <button class="btn btn-danger">Sterge contul</button>
                                </form>
                            </td>
                        </tr>
                        <% }); %>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>
</div>

<%include ../partials/footer%>